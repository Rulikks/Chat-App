import React from 'react';
import { View, Text, StyleSheet, Image } from 'react-native';
import { styles } from '../../styles'

const UserShortcut = () => {
    return (
        <View style={styles.UserShortcut}>
        </View>
    )
}

export default UserShortcut
